# BOB — Audit of docs/PROCESS.md (sixth edition) and docs/PROCESS-MEMOIR.md against the record

**Method.** Read both volumes end to end; re-executed the document's own printed self-checks (sentinel greps, the subsection-index awk command, the split arithmetic, falsifier 4 in targeted form); read `agents/PROTOCOL.md`, all five scripts, all 9 charters, `ORG_CHART.md`, `docs/SPONSOR.md`, the gates, ADR-0016/0017/0019/0020/0021/0022, the posture list, both halt logs, the round-4 verdict, WO-0038, SO-xgmii_rx_64, journal volume headers, and remote refs. Verdicts below are CONFIRMED (re-executed) unless marked otherwise.

**Verified baseline (so the findings are read against it).** A large fraction of the document's load-bearing claims re-execute cleanly: core sentinel count 0 and memoir 38 (33 uses + 5 mentions, reconciled as claimed); the subsection index re-derives byte-for-byte from the printed command (71 headings, every `· n` matching); 4,920/1,640/1,445 line figures exact; 128/127/15/8 posture-list figures exact; halt logs exist with exactly 18/3 and 17/0; sibling `91f005d` descends from `7b749f0`, touches 8 paths, none of either volume; 86 `mut/*` refs on origin in 15 campaign prefixes + 2 probes; 13 seal files; the escape ledger (`docs/reports/audit/dv_escapes.md`) never existed at any commit; INDEX.md untouched since `550df53` (2026-08-01); §2.6's enforcement column is exactly right against the scripts (R9 merge-triviality in CI only, no force-push check in any script, R10 size threshold commit-surface only, R11 on both surfaces, R6 duplicate-refusal CI-side); the SPONSOR.md canary contradiction is cured with a margin as claimed; nine charters carry nine identical numbered sections (my check confirms the core and corrects Sal's Part V inventory, which listed six); SO-xgmii_rx_64 is one PASS preserving two FAILs across four rounds; WO-0038 is ACCEPTED at round 6. The document's mechanical-honesty core holds. The findings below are where it does not.

---

## F1 — CRITICAL · C8 (split integrity), C2, C6 — "Nothing was deleted" is false: at least 14 preserved superseded margins from the fifth edition are in neither volume, and the [CORRECTED] legend's preservation promise is broken for stamps still standing in the core

- **Claims:** Core front matter: "That material is not deleted — **it is now a companion volume**"; split rule S4: "**nothing was deleted.** Every moved line is in the companion"; stamp legend: "`[CORRECTED · C-nn]` … What the superseded claim said, and what refuted it, is **preserved in the companion volume, under the section this text sits in**"; falsifier 4: "a diff of this file against the fifth edition contains **no removal that is not either a moved block or a named repair**."
- **Probe:** `git show d96a5b1:docs/PROCESS.md` (fifth edition, 5,446 ln) contains **50** sentinel occurrences — by its own census, **47 preserved margins** + 3 mentions. The memoir preserves **33**; the core 0. Fingerprinted all 50 against both volumes and grepped distinctive interior strings.
- **Observed:** ~14 fifth-edition margins are in neither volume and in no manifest row, including: §1.6's drill margin ("the first edition stated it as an **exercised fact**") — while the core still carries `[CORRECTED · C-33]` at §1.6, whose legend promises the superseded claim is in the companion under that section (it is not; memoir's §1.6 block carries only the C-32 index margin); §4.7's `[CORRECTED · C-114]` margin ("the first edition said **'four commands'**" — 0 hits in either volume); plus the §1.4 "**the** one" margin, §3's taxonomy-closure margins, the placeholder-token margin, the "spawn token" margin, §3.3's paraphrase margin, §3.4's tally and "two halves" margins, §6.0's "named in §6.0"-promise and "deliberately frozen" margins, and §6.2's case-count margin. B.11 names the deletion of exactly one confession paragraph and its budget line (row 14) — none of the above. B.11's own preamble promises "every applied change lands as a repair … with the superseded text preserved beside it — **now in this volume**." The memoir's own preserved §1.6 margin states the convicting principle: "a `[CORRECTED]` marker means … the first edition's claim preserved beside it, **or it means nothing**."
- **Aggravating:** falsifier 3's line accounting (5,976 → 4,920) closes only against an **uncommitted intermediate state** (fifth edition + sixth repairs, pre-strip) that exists at no commit — the committed baseline is 5,446 — so the split's arithmetic warranty is not independently executable, and the manifest's line numbers resolve against a state no reviewer can check out.
- **Mitigation, stated fairly:** no current-law sentence is affected; several deleted episodes survive as Annex-B ledger narration that predated the split. But the sixth edition's one structural act ships with its printed warranty ("verify it rather than trust it") returning **wrong when actually run**, and two live `[CORRECTED]` stamps now point at preserved claims that do not exist. This is the framework's own definition of CRITICAL: the edition's central structural claim is materially false, found by the falsifiers the document itself prints. (This finding is mine; the framework's probes 6.1/8.1 pointed at the memoir-internal census, which reconciles — the attrition is only visible against the fifth edition.)

## F2 — MAJOR · C2 (probe 2.6), C7 — The record is not conformant to ADR-0020 where the document narrates the reform as complete: the live gate checklist quotes superseded constitutional text under a "verbatim" banner, and four charters plus the gate file still oblige the outlawed N/N ratio

- **Claims:** §3.7: "the checklist **quotes those clauses verbatim** rather than paraphrasing them `[RE · C-90]`"; §3.8: "The packet was right and the rule was wrong; **the rule moved to where the practice already was**"; §1.5/§6.0 export "the anonymized auditor charter" as the kit's exemplary charter ("evaluation criteria written as falsifiable percentages").
- **Observed:** `docs/gates/P1-module-ready-checklist.md` §0.1 quotes, under "quoted here **verbatim**", the **pre-amendment** §7 cell ("auditor's seeded mutations **all killed** by the DV suite") and §3 quotes pre-amendment §10 ("every PASS reports **kills N/N (N ≥ 3 …)**"); its §0.3 row is headed "The mutation **N/N record**". The constitution's cells moved at `a76e485` (2026-08-11 19:21, ADR-0020 act 6), 15 hours after the checklist landed (`61e0c76`), and were never re-quoted — ADR-0020 §10 item 1 names exactly this as an owed act whose trigger ("acts 5–6") **has fired** and is unpaid. The charters are worse: `agents/charters/auditor.md` (§3 "the PASS reports N/N", interfaces table twice, DoD "mutation kills N/N confirmed"), `dv_lead.md` (3 sites incl. DoD "killed N/N"), `tb_writer.md`, `formal_dv.md` all still oblige the ratio the constitution outlaws ("no ratio stands in for the dispositions"), and auditor.md §3 also still teaches the superseded transient-application model ("applies mutations transiently in an uncommitted working tree"). ADR-0020's non-decisions section concedes: "if a later reading finds 'N/N' in that charter text now stale against §10, that is an owed charter diff" — this reading finds it, in four charters and the live gate.
- **The document's failure:** neither the core nor memoir Annex B discloses any of this. B.11 row 22 discloses only the **shell's** packet form (B.2 item 14); B.2 item 13 routes only the constitution's transient-model clause. §1.1's summed-residue table — sold as "that residue summed … the number an adopter actually needs" — carries no row for the stale-restatement debt class at all, and §2.7's own parameter rule ("updates the canonical statement **and every restatement**") is violated by the record while §3.8 narrates the amendment as landed. The auditor charter is exported as the kit artifact "an adopter most needs" without a word that its text contradicts current law in four places.

## F3 — MAJOR · C2, C1 — The constitution contains no R10 or R11: ADR-0017's written §5 diffs were never applied, and the core prints the thirteen-row table as "this program's real ids" without saying its own constitution carries only nine of them

- **Claims:** §2.6: "The ids below are this program's, and they are printed so that the rule/case/citation machinery is executable … **a facsimile that renumbers is a facsimile of nothing**"; §6.0 kit row: "**The constitution template** — §2's rules as a document a program can amend, with the posture declarations already in the clauses."
- **Observed:** `grep R10 agents/PROTOCOL.md` → nothing. PROTOCOL §5 enumerates R1–R9 only. ADR-0017 (ACCEPTED, IN FORCE) §8.2 contains the written §5 diffs ("R3 amended, R5 amended, **R10 added**") and line 534 minted R11 "so PROTOCOL §5's enumeration stays honest" — none applied. The scripts enforce R10/R11 by name; the constitution the kit exports as the source of "§2's rules" lacks two of the eleven numbers every refusal message cites. §2.7's disclosure — "**an instrument** in force whose constitutional diffs are written but not applied" — is singular where the record holds at least two (ADR-0017 §8.2 protocol hunks; ADR-0019 §7.2–7.3 charter hunks), plus ADR-0020 §10's fired owed acts (F2). An adopter running §6.2 act 1d ("write the constitution by reading what your scripts actually refuse") against the reference pair discovers the reference program never did this reconciliation itself, and the document does not say so.

## F4 — MAJOR · C6, C7 — A sentence the program knows to be false stands unmarked in the core, which claims to carry "current law only": Annex A.8's "and nowhere else"

- **Claim:** A.8: "the assignment is declared per seat in the launcher definitions **and nowhere else**."
- **Observed:** Tier is also declared in `ORG_CHART.md`'s roster **Tier column**, in `PROTOCOL.md` §2 ("Leads are spawned as Opus-class agents, workers as Sonnet-class"), and in every charter's Identity section ("Model tier: Opus 5") — and in the core's **own §1.6 roster facsimile**, which prints a `Tier` column, contradicting A.8 inside the same document. Memoir B.11 refusal 3 admits this is "false and **known-false for three editions** now and still unmarked in its running line" — an admission placed in the volume that "states no rule" and that a core-only reader (the only reader the split promises a clean file to) never opens. The same refusal list admits further unrepaired known defects standing silently in the core: the contingent seat's trigger "described as more deterministic than the record has it" (charter says the orchestrator "**may** activate"), §1.6's "two leads" against an index listing three, and §6.2's HALT ids cited bare across two overlapping numbering sequences. Under the document's own posture doctrine — a false claim is worst where the apparatus implies coverage — a known-false indicative sentence with no marker, in a document whose whole spine is truth-in-labeling, is the exact defect class the first edition was convicted of, recurring with a ledger entry instead of a repair.

## F5 — MAJOR · C7, C10 — The split and the warranty re-scope operate on an instrument that binds nobody: ADR-0022 is PROPOSED, NOT IN FORCE, with all three signatures OWED — and the core cites it without its status

- **Claims:** Core front matter cites "`ADR-0022` §4" as where the split's one judgement "is recorded"; §1.4(a) defines normative class 4 as "this document's own **central claim** — what the artifact warrants and to whom", requiring a record **and a separate acceptance**; §3.6: the acceptance act "is what flips the record from proposed to in force."
- **Observed:** `docs/adr/ADR-0022` line 3: "**Status: PROPOSED. NOT IN FORCE.** Nothing in this file binds anything until the countersignature route of §7 completes", with a table of three signatures — dv_lead, auditor, orchestrator's acceptance — **all OWED**, and the admission that "Decision 1 was taken single-seat and uncountersigned by the seat whose work it" grades. So the re-scoped warranty and the split — normative changes by the core's own four-class test — are live while their governing record is unaccepted, the same "proposed-but-operating" shape the core holds up (§3.6, §3.9) as a measured anomaly elsewhere. The disclosure is honest **in the ADR and in B.11 row 25**; the core — the self-described law volume — cites the ADR twice with no status and names the owed confirmations only via a pointer into Annex B. Severity capped at MAJOR because the disclosure exists; it is a finding because it lives everywhere except the volume whose readers inherit the warranty.

## F6 — MINOR · C2 (probe 2.2) — The scope-table facsimile's contingent-seat row is written from the intention, not the artifact

- **Claim:** §2.3 facsimile row: "`<contingent seat>` | as its line's lead, **scoped to the sub-domain it activates for**", under the rule that a facsimile is "written from the real artifact rather than from the intention."
- **Observed:** `policy.sh` gives `rtl_lead_md` the **full** implementation scope (`rtl_lead|rtl_lead_md)` share one case arm: `libs/* top/* bin/* rtl_snapshots/* handoffs/*`), un-narrowed and live while the seat is dormant; the sub-domain narrowing exists only as a plan in rtl_lead's charter ("partition `libs/**` by module list in a committed handoff WO- **at activation**"). Related precision defect: §1.3 claims "Each seat has a charter: a versioned file with nine numbered sections … identical across every seat" re-derived over "all nine charters" — there are ten known agents; the tenth (rtl_lead_md) has no charter file, only a section of another seat's charter, which Annex C discloses but §1.3's re-derivation note glosses over.

## F7 — MINOR · C2, C1 — "Outside its frame entirely" hides that the adjudicator runs, and fails, on every campaign push

- **Claim:** §2.5: the re-check "does **not** adjudicate the marked campaign references of §3.9 … which sit **outside its frame entirely**."
- **Observed:** `.github/workflows/journal-check.yml` triggers on bare `push` with no branch filter — the full-history re-check runs on every `mut/*` push and necessarily fails there (campaign commits are plain `git commit` with no journal coupling, per ADR-0019 §1.1). True as a statement about what binds merges; false as a statement about where the check runs — and the mechanism it omits (routinely red CI on 86 refs, trained-to-ignore failures beside the branch whose "publicly-failing check" A.5 calls the detector) is a real alarm-fatigue hazard in the actual machinery that the document, which elsewhere demands every stamp name its surface, never mentions.

## F8 — MINOR · C7 — A measured stamp is lent to post-measurement growth inside its own block

- **Claim:** §3.9: "one citation covers the whole block `[RE · C-104]`" over the fifteen scoring bullets.
- **Observed:** the posture list's own appended Note 3 (2026-08-12): "**C-104**: the referent grew from twelve bullets to fifteen, and the row's own count was off by one." The three added bullets carry the measured row rather than the "(no posture row — added after the measurement)" marker the document applies scrupulously elsewhere — the precise "stamped sentence lends its authority to its neighbour" hazard the boundary block names, here inside a stamped span where the seam column cannot see it.

## F9 — MINOR · C6, C10 — Small decay and parameterization gaps in the volume that preaches re-measurement

- §6.3's "**twenty-one** numbered decision records" is stale at the sixth edition's own commit: ADR-0022 landed in the same round, making 22 — a count not re-measured in the act that moved it, the document's own most-convicted class.
- Both size gates read their thresholds from the **environment** (`JOURNAL_SOFT_MAX`/`JOURNAL_HARD_MAX` in policy.sh:13–14, `AGENT_COMMIT_BLOB_MAX` in agent_commit.sh:203 **and** check_journals.sh:30). §2.2/A.3/A.7 present the numbers as anchored parameters; that they are override-able per invocation on both surfaces — including by the CI environment — is a mechanism the document omits (partially mitigated by the generic "the gate is a wrapper somebody invokes" disclosure and by check_journals' comment that CI catches local overrides at its own default).
- §6.2 cites HALT-01…HALT-16 from **two overlapping numbering sequences** (run one and run two) without run tags — memoir-admitted (B.11 refusal 3), unrepaired in the core.

---

## Score vector (Sal's ten criteria, 0–4, never averaged)

- **C1 Comprehensiveness: 3.** Coverage of the inventory is genuinely near-total (rotation/chain, R11 carve-out, transcription exception, seeding, short-ids, numbering, dispatch-only, seals, disposition algebra, LH1–LH3, genesis, rehydration — all present and mostly accurate). MAJORs are the omissions in F2/F3 (stale binding artifacts; the constitution's missing R10/R11) — disclosed nowhere.
- **C2 Accuracy: 2.** The machinery claims are excellent (I could not manufacture a false `[MC]` either; every enforcement-column cell re-executed true). Undisclosed false-of-the-record present-tense claims survive: F2 (C-90's practice), F4 (A.8), F6, F7.
- **C3 Human readability: 3 (PLAUSIBLE, single-reader).** Dense but navigable; the derived index works; the doubling rule pays off. The 590-line preamble toll and stamp/pointer traffic are real costs; no defeating sentence found in the sections I worked operationally.
- **C4 AI readability: 3.** Legend closure holds in my sweep (two inline-disclosed deviations from stamp grammar in §6.0/§6.2); the index re-derives; the backtick/bracket distinction holds. Not fragment-tested (probe 4.1 not run).
- **C5 Replicability: untested here (probes 5.2/5.3 out of my scope); on paper 2–3** — the halt-log repairs named in B.11 rows 13–17 are real in the text; the with-shell path remains unmeasured and honestly so.
- **C6 Self-compliance: 2.** Every printed self-check I ran passes (sentinel, index, arithmetic-as-arithmetic, dialect count, noun census). But F1's falsifier 4 fails when actually executed, F4 is an internal contradiction (A.8 vs §1.6's facsimile), and F9's count decay recurs.
- **C7 Calibration: 2.** The apparatus is real and mostly honest (the summed-residue table, the boundary block, the pin wording are exemplary). Undisclosed MAJORs: F2's clean-record implication; F4's unmarked known-false sentence; F8.
- **C8 Split integrity: 1.** F1: the lost-nothing warranty is materially false under its own falsifiers; the memoir is otherwise genuinely rule-free and the precedence rule is executable, but a dropped-history defect invisible to readers of either volume is exactly what this criterion exists to catch.
- **C9 Portability: 4.** Noun census over §1–§6 + Annex A: clean except enumerated deliberate exceptions (two repo addresses, ADR-0022, `mut/wo-0039` inside a facsimile, opus/sonnet in A.8 where naming this program's tiers is the annex's job). Name map (Annex C) round-trips correctly against real paths, including the honest "never created" row for the escape ledger.
- **C10 Maintainability: 2.** Fact-once and pointerization are real; but the record shows the class this criterion fears operating at full strength: ADR-0017/0019/0020 all hold written-but-unapplied or fired-but-unpaid restatement debts, the document's own governing ADR is unaccepted (F5), and stale counts recur within one edition (F9).

**Where I diverge from the framework:** Part V's charter inventory ("shared section shape" of six items) is wrong — all nine charters carry nine identical numbered sections, exactly as §1.3 claims. The framework's known-lag pointer (dv_lead's N/N) understates the population: the lag spans four charters **and the live gate checklist's declared-verbatim quotes** (F2). F1 (fifth-edition margin attrition), F3 (constitution lacking R10/R11 as an undisclosed kit defect), F4 (the memoir-buried known-false A.8), and F7 (CI running red on campaign refs) are findings my own record-reading surfaced beyond the framework's probes.

**One-line verdict for the council:** the document's enforcement-honesty core survives hostile re-execution better than any artifact of this class I have seen — and this edition's signature act, the split, fails its own printed warranty (F1), while the reform it narrates as landed (ADR-0020) is still unlanded in four charters and the live gate (F2). RETURN on F1 and F2; every other finding is condition-grade.
