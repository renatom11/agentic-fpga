CHARLIE — COLD-READER AUDIT OF docs/PROCESS.md (sixth edition)
Seat: R2-equivalent, quarantined. Files read: /home/user/agentic-fpga/docs/PROCESS.md (4,920 lines, in full) and /home/user/agentic-fpga/docs/PROCESS-MEMOIR.md (in part: Part 0, Part I samples, Annex B head + B.0–B.2, Annex C, Part IV). No other repository file was opened; every verdict below is textual. Where the framework's criteria apply to a cold reader I cite them; findings the framework did not predict are marked OWN-JUDGMENT. Note for the council: the memoir's B.1 confirmation matrix carries a standing row "The whole text, read cold ... edition 6: owed" — this report is a discharge candidate for that row.

SELF-CHECKS THE DOCUMENT PRINTS, RUN WHERE RUNNABLE FROM TEXT ALONE (method note, not praise): dialect count = 12 over its own list (holds); subsection index = 71 headings (holds — I counted the printed list); split arithmetic 330+752=1,082 and 4,858+62=4,920 (internally closes); S1 32+3(S3)=35 blocks (matches the memoir manifest); the memoir's sentinel census reconciles its own 38-vs-33 grep gap explicitly (Part 0 §0.5); probe 3.2 timed retrieval: 8 of 10 questions answered inside budget via the subsection index — the two misses are Findings 13 and 19. The falsifiers I could not run (grep, diff-against-fifth-edition) are R1's.

================ FINDINGS, MOST SEVERE FIRST ================

F1 — CRITICAL — C3 (probe 3.6) / C5.3. The seeded-defect scoring law contradicts itself on where a negative control sits, so a cold reader cannot produce a determinate gate tally.
Anchors: §3.9 (PROCESS.md ~3268–3291) and §3's sign-off facsimile (~2218–2224).
The claims, quoted: §3.9 defines the columns — "*Sealed* is every defect class the manifest sealed... *Seeded* is the subset actually rendered as sealed and run." It then says "**The known grounds for a class sitting in one column and not the other** are three" and lists as Ground 3: "**negative control.** A class **seeded deliberately** so that a *named assertion stays green*... Its seal predicts the green and declares in advance that the class scores nothing." §3's facsimile agrees with the grounds list: "**The difference, itemized**: each class in sealed-and-not-seeded ... never rendered | unscoreable | negative control | <other>".
Probe: Sal's synthetic tally (sealed=7, seeded=5, one negative control, one never-rendered, one campaign survivor), attempted after one careful reading.
Observed: two irreconcilable readings. A negative control is by Ground 3's own words "seeded deliberately," and it is rendered and run — so by the printed definition of the Seeded column it belongs in Seeded; by the grounds list and the facsimile it belongs in the sealed-and-not-seeded difference. Depending on the reading, my tally's Seeded column is 5 or 6 and the difference list has one or two members. Expected: one derivable tally. Aggravator: the adjacent rule "A ground that turns on what the seal disclosed holds only where the disclosure was frozen before the run... Grounds 2 and 3 are both of that kind" (~3296–3299) is itself hard to operationalize — Ground 2 (unscoreable) is decided by inspection after the run, so which part of it "turns on what the seal disclosed" is left to reconstruction. This is the densest law in the book; §3.9's own sentence says "the only thing standing between these rules and a wrong number is a reader who knows what each is for," and the text gives that reader two incompatible column definitions. The document convicts exactly this defect class in §5.2 ("a column's name is not its definition") and in §3.9's first exhibit — and recommits it in its own running text. Verdict: CONFIRMED (textual contradiction; which reading the real record uses is checkable only by R1, whom I ask to adjudicate against a committed sign-off).

F2 — MAJOR — C8.3 / C7.3 / C7.5. The conditions that currently gate the document's central claim live only in the memoir, and the memoir declares itself non-current on its first page.
Anchors: PROCESS-MEMOIR.md lines 10–11 and B.0.4 (~715–726); PROCESS.md §6.0 (~4230–4236).
Quoted: the memoir's opening — "It states **no rule**. Nothing here binds any seat, and **nothing here is current**." Then B.0.4: two rows marked "**OPEN — CONDITION**", set by the fourth council as "a **gate on any future FIT-at-unit-level**... **before any unit-level replication claim is certified**" — the doc–shell drift check (B.0.4a, "now four editions old") and the with-shell alien-domain run (B.0.4b). Also B.1: a live table of owed confirmations ("edition 6: owed"); B.2: live amendment candidates with owners.
Observed: (a) the volume that says nothing in it is current carries the program's only statement of its currently open, verdict-gating conditions, its owed-confirmation matrix, and its amendment queue — present-tense governance state, not tense-history; a stale B.0.4 row would be exactly the "competing law" the split's honest-residue paragraph says a drift "can only ever" not be. (b) The core partially discloses B.0.4b in its own words (§6.0: "Closing it needs a with-shell adoption run by a party this program did not commission, and that is a condition on any future claim about the unit") but nowhere states that the drift check is likewise a certification gate — a core-only reader learns the drift check "does not exist" and is "owed," not that a council verdict blocks the unit-level claim on it. Expected: current gating conditions statable from the core, or the memoir's self-description qualified. Severity note: partially disclosed, so capped at MAJOR under the self-disclosure rule; the memoir's flat "nothing here is current" against its own OPEN-CONDITION rows is an inaccurate self-description and I flag it to R1/council for the C7-CRITICAL escalation test. Verdict: CONFIRMED (both passages quoted).

F3 — MAJOR — C5 / C9.3 — OWN-JUDGMENT (framework's 9.3 is adjacent but did not predict the circularity). Step 0 reads Annex C; the fork contract deletes Annex C as the first act of Step 0.
Anchors: PROCESS.md §3.9 equivalent-defect bullet (~3329–3336), §6.1 fork table (~4329), §6.2 Step 0 (~4438–4451); PROCESS-MEMOIR.md Annex C header (~1525–1531).
Quoted: §3.9 — "The pairing is carried in the name map, **Annex C**, which is what §6.2 Step 0's cross-repository greps read." §6.1's Annex C row — "**Delete whole and write your own**, as the first act of Step 0. It maps this document's function names onto **this** program's seats, paths and tokens; yours are different." The memoir's Annex C — "It exists for one reader: the one executing §6.2 Step 0."
Observed: at Step 0 the adopter is tracing this document's function names into the SHELL's machinery, and the shell's tokens are this program's (§6.0: the policy module "hard-codes this program's nine seat names"). So the original Annex C is the lookup Step 0's greps need, and the fork's first act of Step 0 destroys it; the replacement the adopter is told to write ("yours are different") presupposes seats that do not exist until Step 1. The two instructions are sequenced against each other in the core's own text. Expected: the fork operation on Annex C ordered after Step 0's trace, or the deletion instruction scoped to post-trace. Verdict: CONFIRMED (textual).

F4 — MAJOR — C5.4-adjacent — OWN-JUDGMENT. The acceptance rule for decision records is circular for the highest-stakes case: an orchestrator-proposed constitutional amendment.
Anchors: §3.6 (~2903–2909), §2.7 items 1–3 (~2006–2014), §5.7 (~4037–4039).
Quoted: §3.6 — "**A record proposes; a different seat accepts.** ... for anything touching the constitution, the charters or the enforcement scripts it is the orchestrator, because those are the files it alone can stage; for anything else it is the seat named in the record's own deciders block, **which must not be the proposing seat**." §2.7 — "an orchestrator journal entry accepting it."
Observed: the "must not be the proposing seat" clause grammatically attaches only to the "anything else" branch. If the orchestrator proposes a constitutional change, the text as written has the orchestrator accepting its own proposal — the exact "permission slip" §5.7 lists as a disguise of the root class ("An instrument that authorizes its own author to make the change it proposes"), whose stated cure covers only the *applying* act, not acceptance. Nothing in the document says whether the orchestrator may propose, or who accepts if it does. Expected: the case stated, either as a prohibition on orchestrator-proposed amendments or a routed acceptance. Verdict: PLAUSIBLE (the constitution may close this; I am barred from reading it — R1 should check PROTOCOL §11).

F5 — MAJOR — C7.1 / C6.3. §1.0 stamps the spawning monopoly review-enforced; §1.1 says three lines later that this posture is wrong — with no [CORRECTED] marker — and the row citations shuffle between the two sites.
Anchors: §1.0 (~611) and §1.1 (~619–641).
Quoted: §1.0 — "Sole-spawner and sole-committer: `[RE · C-01, C-06]`." §1.1 — "The orchestrator is the **sole spawner** ... and the **sole committer** ... `[RE · C-06, C-07]`" followed immediately by "**The spawning monopoly is held by the substrate, not by a rule and not by an instrument.** ... Its posture is therefore *not* review-enforced — it is a **substrate fact**."
Observed: a stamped [RE] claim contradicted in its own section's next bullet, using none of the apparatus built for exactly this (no [CORRECTED], no "(no posture row)" reframe); and the same two-limb claim cites row-sets {C-01, C-06} at one site and {C-06, C-07} at the other, so a reader cannot tell which row decides which limb. The document's stamp-grammar lecture ("A stamp on a compound sentence is a stamp on its strongest limb") sits in the same section as a compound stamp it does not repair. Expected: the spawning limb re-stamped as substrate-fact/no-row, consistently at both sites. Verdict: CONFIRMED (textual). For R1: re-execute C-01/C-06/C-07 against the posture list to see which attribution is the transcription error.

F6 — MAJOR — C3.2 / C4.3. HALT-numbers are cited from two different logs with overlapping number spaces, mostly without saying which log.
Anchors: §6.2 throughout — HALT-01 (~4448), HALT-02 (~4704), HALT-04 (~4576), HALT-05 (~4536), "HALT-05's sub-problem, and HALT-06 **of run two**" (~4561–4563), HALT-10/11 (~4613), HALT-12 (~4401), HALT-14/15/16 (~4652); Annex A.6b HALT-08/09 ("From adoption run one's own environment", ~4840).
Observed: two halt logs exist (run one: 18 halts; run two: 17), each evidently numbering from 1 — proven by the one disambiguated citation "HALT-06 of run two." Every other citation is bare. A reader sent to verify HALT-04 or HALT-14 does not know which committed log to open, and the numbers collide across logs. Expected: every HALT citation qualified by run, or a single shared number space declared. Verdict: CONFIRMED (textual ambiguity; log contents unverifiable from my seat).

F7 — MAJOR — C5.3 / C3. The scope-table facsimile contains a row its own declared format cannot express, sitting unmarked inside the machine-checked table.
Anchors: §2.3 format spec (~1704–1710), facsimile (~1716–1730), residue note (~1743–1748).
Quoted: the format — "one row per seat, whose cell is **a list of path prefixes** that are *allowed*, optionally with narrower prefixes carved *out* of them." The worker row — "its line's tree, narrowed further by its own packet; packets/** **(its own return log only)**."
Observed: "(its own return log only)" is sub-file granularity — a return log is a section inside a packet file, and "its own" is per-spawn — which no path-prefix allow-list can encode. The nearby residue note discloses that per-packet narrowing is review-enforced, but the return-log parenthetical is a second, different unexpressible constraint presented inside the facsimile as if it were table data — precisely §5.4's shape ("an unmarked assertion inside a verified block borrows the block's authority"), and the facsimile's caption says "the *shape* is the mechanism." An adopter transcribing the table into the policy-module format meets a cell they cannot compile and must guess whether to widen (grant packets/**) or invent enforcement. Expected: the parenthetical moved out of the cell and named as the review-enforced residue it is. Verdict: CONFIRMED textually (whether the real policy.sh grants bare packets/** is R1's to check).

F8 — MAJOR — C3.1. Load-bearing tokens and terms are used hundreds of lines before their legend, or never defined; the dialect's own admission criterion does not explain its exclusions.
Anchors: opening paragraph (lines 17–20) vs the annex-pointer legend (~407–421); line 48 vs ~156; §1.2 (~811); §1.1 (~683, ~714); "kit" (~313, §1.5 ~1202, §6.0 table ~4181); dialect head (~317–320) and "How to read this" (~446–450).
Observed, as a logged cold-read term list: (a) backticked pointers `[B.2·9]`, `[B.9·2]` appear in the document's third sentence; the legend explaining them is ~390 lines later, and until then they are indistinguishable from citations a reader is failing to parse. (b) `C-nn` is used at line 48 before the explanation at ~156. (c) "kit" is never defined anywhere — a reader cannot determine from the text whether "the kit" is the shell, the shell's contents, or the export unit (the §6.0 table header "Kit artifact" with column "where its original lives: this repository" makes it actively ambiguous). (d) "launcher/launcher definitions" first carries weight at §1.2 ("carried by the launcher definitions, which name a tier per seat") and is only characterized at §6.0. (e) "trunk"/"working branch" are used from §1.1 and §1.8 but only explained in §2.6's branch-topology paragraph. (f) "ratification" is leaned on at §1.1 (~714, the summed table's epoch) three sections before §1.7 defines the act. (g) The dialect's stated membership rule — terms "used in more than one section" — is satisfied by gate, packet, seal, charter, journal, kit, launcher, none of which are in it, so the totality claim ("the list below is the whole of them") is true of the list and uninformative about the criterion; a reader cannot predict what the dialect will and will not cover. Expected: legend before first use (the document's own §5.8 logic — the apparatus needed "before comprehension" belongs at the top); "kit" and "launcher" bold-defined. Verdict: CONFIRMED.

F9 — MINOR — C3.5 — structure. The split apparatus precedes the reader's orientation.
Anchor: lines 33–129 vs "Read this first" at line 131.
Observed: the first ~130 lines a cold adopter meets are the edition anchor, sibling-landing forensics, and four falsifiers for a fifth edition they have never seen — genealogy-first, in the edition whose one structural act was removing genealogy from the reader's path. "Where to go" rescues it at line 133, and probe 3.5 (acting from "Read this first" + Contents alone) degraded retrieval only mildly in my run — but the toll begins before the tollbooth is signed. Expected: the split block after "Read this first," or a one-line deferral at the top. Verdict: CONFIRMED; MINOR because the skip route exists and is honest.

F10 — MINOR — C6.3 (observable cold) / C3. The packet table falsifies its own summary sentence on relay class.
Anchor: §3 (~2126–2141).
Quoted: the finding row's relay class — "**Verbatim** when the auditor's" — three lines above "Relay class is a property of the packet **type**, not of the round."
Observed: a class conditional on the author is not a property of the type; the table's own last column explains the real source (the relay clause), but the summary sentence stands unqualified. Expected: "of the type (with one authored exception, the finding)" or equivalent. Verdict: CONFIRMED.

F11 — MINOR — C4.2. The stamp grammar is not closed: hybrid forms defeat the parser the legend invites.
Anchors: §6.2 (~4345–4347) — "[P1 · 2026-08-11/12 and 2026-08-12 — no posture row; both performed after the measurement]"; §6.0 (~4272) — "[PLANNED — no posture row; the instrument does not exist]".
Observed: the legend defines six bracketed forms, all of the shape [TOKEN · ... · C-nn]; these two carry prose, date-ranges and conjunctions inside the brackets. The backtick-vs-bracket distinction survives everywhere I checked, but a ≤20-line parser for the six legend forms flags these as violations, and a fragment-fed agent (C4.1's declared consumer) meeting "[P1 · ... and ... — no posture row...]" has no rule for it. Expected: no-row stamps given a defined seventh form, or the prose moved outside the brackets. Verdict: CONFIRMED.

F12 — MINOR — C3 / C4.3. Annex A's ordering and labeling: A.6b is listed and placed before A.6, and its content extends A.5, not A.6.
Anchor: subsection index (~585–586) and Annex A body (~4825, ~4842).
Observed: "A.6b — There is no reachable hosting platform at all" precedes "A.6 — The only environment that can run the suite runs on pushed references"; the "b" suffix implies a sub-case of A.6, but the content (no platform → A.5's protections and §6.2 Step 3 fall to convention) is a limiting case of A.5. A reader navigating by label files it in the wrong place; nothing explains the numbering (evidently a later insertion that avoided renumbering — but the document elsewhere narrates exactly such choices and here does not). Verdict: CONFIRMED.

F13 — MINOR — C3.2 retrieval detour — OWN-JUDGMENT. The preconditions for a PASS sign-off are never collected in one place.
Anchors: §3.8 (~3046–3064: the floor), §3.9 (~3138–3145: campaign placement as a precondition), §1.4(e) (~1049–1063: the anchor-before-first-verdict ordering).
Observed: probe 3.2's question "when may a sign-off say PASS" required assembling three sections; each carries a necessary condition and none points at the full set (§3.8 does cite the other two, but as list elements, not as preconditions of issuing). Expected under the fact-once rule: one statement site for the precondition set, pointers elsewhere. Verdict: CONFIRMED as a detour, not a gap.

F14 — MINOR — C6.3 (reported cold, for R1). Fact-once violations visible without the repository.
Observed: the 128/127/15/8 measurement is stated in full at "Read this first" (~150–157) and again in full at §6.1 (~4299–4301), with a third partial statement at §2's intro (~1486–1490); packet-number allocation by the sole committer is stated at §1.1 (~677–678), §3 (~2226–2229), and §3.2 (~2413–2415). Under the document's own rule ("A fact ... is stated in full exactly once ... everywhere else as a one-line pointer") the second full sites are findings even though consistent. Verdict: CONFIRMED.

F15 — MINOR — C3.4 sentence-level load, with the two worst exhibits named.
(a) §5.7's stamp-explanation italic (~4015–4020): "*Four of the six below are anchored to incidents in this program's record; two are marked, because an audit of this document could anchor the codification but not the episode `[P1 · 2026-08-11 · C-125]` — the date being the anchoring measurement's own, not any one episode's, and the stamp carried none at all until the census that reads every stamp against its row noticed that this one had skipped the field its own grammar requires*" — a stamp, its meta-history, and a census anecdote braided into one sentence; I needed four reads and am still unsure what C-125 decides. (b) The opening sentence (lines 13–20) carries two annex pointers, an em-dash chain, and the unit's central warranty in one breath. Both defeat two readings; both are load-bearing locations. Verdict: CONFIRMED (sample of 20 paragraphs: most passed; these two and §2.7's split list are the structural failures).

F16 — MINOR — C3 / C6. §2.7's "four things" list is delivered as three items, thirty lines of prose, then item 4.
Anchor: §2.7 (~2000–2045).
Observed: "requires **four** things" heads a numbered list of three; item 4 arrives after two intervening rule paragraphs, introduced by "The fourth requirement, stated here rather than above." The reason given is real (only item 4 is this document's own), but a skimming reader — the reader §5.8 says prohibitions are placed for — leaves with three. Expected: four numbered items with item 4 carrying its provenance note inline. Verdict: CONFIRMED.

F17 — MINOR — C3 / C6 unit hygiene. "Fifteen campaign prefixes" against the facsimile's single reserved prefix.
Anchor: §3.9 (~3147–3173).
Quoted: the marking facsimile — "It is a **reserved name prefix**, one segment, used for nothing else" (e.g. mut/) — then the population paragraph: "eighty-six references ... grouped under **fifteen campaign prefixes** and two probes."
Observed: by the facsimile there is one prefix; the fifteen are campaign-id groupings under it. In the one section that lectures "read the unit carefully" and rules a reference population cannot be a denominator, "prefix" is used for two different units two paragraphs apart. Verdict: CONFIRMED.

F18 — MINOR — C8 (cross-volume, for R1). The two volumes disagree on the core's own line count in the cell that governs the evidentiary boundary.
Anchors: PROCESS.md boundary block (~263: "4,920 lines"); PROCESS-MEMOIR.md B.2 item 8 (~864–870: "the core is now **4,919 lines**").
Observed: the memoir cell even narrates having reconciled a previous two-figure disagreement in this very passage and names the core canonical — and still prints a figure one off the core's. Precedence resolves it (the memoir yields), but it is a live specimen of the drift the split's honest-residue paragraph says nothing mechanical prevents, in the most boundary-sensitive cell either volume has. Verdict: CONFIRMED (both quoted; which figure is true of the file is R1's wc -l).

F19 — MINOR — C1-adjacent coverage gap, visible cold — OWN-JUDGMENT. No route exists for seat failure, replacement, or retirement.
Observed: probe 1.2(e)-shaped question, unanswerable from the document: refusal is covered (§4.8), a worker's bad output is covered (bounce, §3.2), onboarding and dormancy are covered (§1.2), death of a session is covered (§4.5, §1.6) — but nothing states what happens when a lead persistently fails, how a seat is retired or its charter deactivated, or who decides that. Contingent seats have activation triggers; nothing has a deactivation route. E2 ("dropping ... a role") names the sponsor as approver but no internal procedure. Verdict: PLAUSIBLE gap (may live in the constitution; the document neither covers it nor declares the exclusion with a pointer, which is its own rule for exclusions).

F20 — MINOR — C7.1 framing. The preamble compresses run two's result in the pessimistic direction, then §6.2 reverses the impression.
Anchors: lines 14–17 vs §6.2 (~4356–4367).
Quoted: preamble — "a second adopter, executing the revised order in a world with no shell, **still invented every line of that layer's logic**"; §6.2 — "**every grammar, identifier, threshold and rule number transferred as a facsimile** ... What run two still invented was the executable layer's *logic*."
Observed: both are true ("logic" vs interfaces), but a first reader takes from the preamble that nothing improved between runs, and only 4,300 lines later learns "the composition changed completely." Calibration whiplash rather than falsehood; the weakest-true-claim rule is technically obeyed and rhetorically not. Verdict: CONFIRMED as framing.

F21 — MINOR — C3. An unresolvable in-document cross-reference in A.9.
Anchor: Annex A.9 (~4898–4899): "*(The other two of the four are stated in the text, at §3 and §3.2, because the record had them.)*"
Observed: "the four" is never enumerated; a reader cannot recover which four parameters were "called stated while stating nothing" or which two now live at §3 and §3.2 (my best guesses: the packet path tokens and the revision-letter semantics — guesses being the point). Verdict: CONFIRMED.

F22 — Observation, no severity — C8.3 forced-dereference census from my quarantine, for R1's 8.3 tally: (a) the sentinel token needed for falsifier 1 is printed only in the memoir — justified by construction (printing it in the core would break the zero-count falsifier) and disclosed; (b) owners/triggers/status of every owed act ("Annex B") — see F2; (c) the doc→shell name map needed by Step 0 — see F3. Genealogy dereferences were frequent and legitimate; these three are current-law needs.

================ PARTIAL SCORE VECTOR (cold-reader seats only; others declined as outside my access) ================
C3 (owned): 2 — retrieval, navigation, and the rule/failure-class doubling largely succeed (the subsection index is the best navigation instrument I have met in this document class), but probe 3.6 fails outright on an undisclosed contradiction in the densest law (F1), and the term/legend ordering (F8) taxes exactly the reader the edition claims to serve.
C7 (co-lead, text-visible half): 3 — the apparatus is unusually honest and the warranty stays unit-scoped at every restatement I checked (opening, §6.0, §6.1, §6.2); the MAJOR defects (F2, F5, F20) are partially disclosed or resolvable by the document's own instruments, but F5 is an unrepaired posture contradiction the apparatus was built to prevent.
C4 (advisory, text-visible): 3 — pointer/stamp lexical separation holds everywhere I sampled; the hybrid stamp forms (F11) and HALT ambiguity (F6) break machine-parseability at the margins.
C5 (advisory, paper-walk only): the genesis walk (§1.7 + §6.2 Step 1) discharges its circularity on paper — the founding-range declaration is the best-specified hard part of the book — but F1, F3, F4, F7 are the stalls a blank executor hits; not scored, probes belong to R3.
C8.3 (my input to R1): 2 for the slice I can see — F2 is a mis-sort by the split's own tense rule; F18 is live cross-volume drift in the boundary cell.
C9 (advisory): 3 — noun quarantine held under my census of §1–§6 (the enumerated exceptions — the two repository addresses, the posture-list path, opus/sonnet in A.8's substrate idiom — are declared where they occur); no unenumerated program noun found in the body; F3 is the portability defect that matters.

CONVERGENCE BAIT FOR THE OTHER SEATS: F1 → R1 should read the real sign-off's seeded-defect block to see which column its negative control occupies (the answer convicts either the column definition or the grounds list). F5 → re-execute C-01/C-06/C-07 row attributions. F6 → open both halt logs and state whether the number spaces collide as I infer. F7 → read policy.sh's worker row. F4 → PROTOCOL §11's acceptance wording. F18 → wc -l at this commit.

Files: /home/user/agentic-fpga/docs/PROCESS.md, /home/user/agentic-fpga/docs/PROCESS-MEMOIR.md.
