# BOB — Audit of docs/PROCESS.md (fifth edition) against the record

## 0. Scope and method

I read all 5,446 lines of `docs/PROCESS.md`, the full `agents/PROTOCOL.md` (450 lines), all five enforcement scripts, and I re-executed the document's checkable claims against the repository: scripts, journals (volume chains), charters, handoffs, gates, ADRs (0015–0021), `ORG_CHART.md`, `tasks/BOARD.md`, `agents/journals/INDEX.md`, `docs/SPONSOR.md`, the posture list, the council/cold-reader/halt-log artifacts, git history, and the remote's `mut/*` reference population.

**What reproduced (the calibration the findings sit against).** This edition's mechanical self-claims are almost eerily exact: the sentinel census (`grep -c` → **50**, as claimed); the derived subsection index (**86 headings**, and every per-section seam count `· n` reproduces from the printed awk command, all 86 values identical); the annex-pointer count (**70**: 28+24=52 into B.8/B.9, 16 into B.10, 2 into B.2 — exact); **86** `mut/*` refs on the remote, none merged, grouping consistent with "fifteen campaign prefixes and two probes"; **13** seal files, **10** scored campaigns each with a `…-VERDICT` under `dv_lead`'s own entry id, **3** DRAFT-unscored (WO-0074/0076/0077) — §3.3's withdrawn-scorer-rule story checks out verbatim; **9 charters × 9 identical sections** matching §1.3's list exactly; self-test runs **51 passed, 0 failed** ("grown to fifty-one cases" ✓); **4** `architect_docs_lead` commits touching `docs/gates/**` (§3.7's probe ✓); WO-0048/0051/0052/0053 never existed at any commit (§3's "four dispatch-only identifiers" ✓); `docs/reports/audit/dv_escapes.md` never existed in history (§1.5 item 5 ✓) while `agents/charters/auditor.md:28` names it with the exact fields; the posture list exists at its stated path with 128 rows, 15 FALSE / 8 PLANNED, and the C-126 12/4 discrepancy exactly as B.5 describes; ADR-0021 is still **PROPOSED, NOT IN FORCE** (B.3 ✓); the CI re-check has **no journal-size limb** and the commit gate does (the §2.2/§2.5 asymmetry ✓); R9's branch limb has **no script anywhere** (§2.6 ✓); PROTOCOL carries exactly **3** labelled `Enforcement:` tags (§5.5's "three" ✓); G0-checklist item 9 documents rulesets + empty bypass + live fire (§2.5 ✓); run-2 halt log says **17 halts / 0 stops** ✓; cold-reader grades and report-card cells are quoted **verbatim** ("accurate, navigable, and not yet hospitable"; "roughly a B-minus"; "undercut by the drift check being named in two editions and built in neither" — all present in the cited files); the `RE-ISSUED (rev B)` state line is real (WO-0080) and `RV-<n>B-VERDICT` exists; HT-01 shows the first harvest across five seats; AUD-0001's corrected tally is 1C/7M/7m/2N. My independent enforcement-tier matrix (Sal Exercise B: 14 claims sampled, incl. C-02/03/09/13/15/21/25/26/37/45/46/47/59/67) found **zero enforcement-tier upgrades — no false [MC], anywhere I probed**. Under Sal's rule 6, that is the single most important negative result of this audit: the error class with invisible damage is absent.

The findings below are what survived that base. Ranked by severity; each carries its criterion tag and its contradicting artifact.

---

## FINDINGS LEDGER

### F1 — MAJOR · C2.4 / C9 (cross-artifact) — The sponsor guide omits the sponsor's fifth standing power, the exact defect class §1.5 convicts, uncured in the source and unrouted
**Doc:** §4.7 — "five standing items … 5. **candidate-by-candidate refusal at the lessons harvest** (§3.10) — the human gate on what leaves the program", with a margin note convicting the second edition for omitting exactly this item; and §6.0's kit table — "**The sponsor guide** | §4.7's surface, in the sponsor's language | this repository, sponsor file."
**Ground truth:** `docs/SPONSOR.md` — which opens "this page is the complete list of what is genuinely yours" — contains **no mention of the harvest, the refusal power, or ratification**: `grep -i 'harvest\|lesson\|refuse'` returns nothing. It enumerates only the six escalation classes, the one-time branch-protection duty, the canary power, the spot-check steps, and "what is NOT yours." PROTOCOL §7 grants the power ("the sponsor may refuse a candidate") and HT-01 shows it being exercised via the inbox-PR form.
**Why MAJOR:** This is §1.5's own named class — "*a contradiction cured in the copy and left in the source is not cured: the source is what its own audience reads*" — applied to the one human check on the export path, and the seat had no excuse of scope: B.7 row F-0025-F states the guide "is inside the specification lead's write scope" and was edited **in this same arc** for the canary correction. §4.7 claims its list is "complete as of this edition"; the kit table asserts the guide carries "§4.7's surface"; neither B.1 nor B.2 routes the divergence. The holder of the refusal power reads a "complete" list that does not contain it. *(My own finding; framework hook: C2.4 record conformance, C7.4 trust-boundary.)*

### F2 — MAJOR · C10.3 / C9.5 (also C2.2 currency) — A false constitutional clause the document itself identifies is the only one of its kind not routed as an amendment candidate
**Doc:** §3.9 margin — the constitution's transient mutation model is "wrong against this program's record … **because the constitution still says it too**." **Ground truth:** `agents/PROTOCOL.md` §10 still reads "the **orchestrator applies each manifest transiently in an uncommitted working tree**, runs the DV suite against it, reverts fully" — while every one of the record's ~15 campaigns ran as commits pushed to the 86 never-merged `mut/*` refs (ADR-0015 made local runs impossible; Annex A.6 says so).
**The defect:** Annex B.2 routes the two directly analogous false constitutional premises — the R1 disjointness claim (item 1) and the §7 gate-scope claim (item 2) — as amendment candidates with a named batch. The §10 transient-model falsehood gets a margin acknowledgment in §3.9 and **no B.2 row, no owner, no closing event**. By the document's own law (§5.5: a routing rule is enforced by "a named owner, a named triggering event and a visible debt"; Annex B intro: "a debt is only enforceable where a later reader is obliged to see it"), a disclosed-but-unrouted constitutional falsehood is routed to nobody. An adopter executing §6.2 act 1d ("write the constitution by reading what your scripts actually refuse") from the kit's constitution template inherits a clause its own program's record has contradicted in 86 counted instances. *(My own finding.)*

### F3 — MAJOR · C2.4 (record conformance; C6.3-adjacent) — §3.3 states the seal's "single mutable state line, flipped at unsealing" as the artifact's form; the record's dominant and current practice never flips it
**Doc:** §3.3 — "A sealed prediction file carries: its state **(and the single line that is flipped at unsealing)**"; constraint 1 — "the file **discloses its own single mutable point**"; the facsimile's `**State**: **UNSEALED <date>** … This state line is the only line of this file that has been altered since the freeze."
**Ground truth:** Of the 13 seals, only the four early ones (WO-0039, -0041, -0050, -0055) carry `UNSEALED <date>` state lines. **At least six campaigns that were scored** — WO-0045 (`State: FROZEN`, campaign "RETURNED — PASSED 5/5"), WO-0058, WO-0061, WO-0063B, WO-0066, WO-0073 (all `State: **FROZEN — unopened.**`, all with committed `…-VERDICT` sections quoting their sealed cells, e.g. WO-0058-VERDICT "two sealed cells falsified") — never had their state line touched. The later practice is zero-edits-ever, with unsealing recorded in the campaign packet's verdict.
**Why it matters:** The error direction is safe (the record is *stricter* than the description), but this is the exact class the document convicts in this very section's margin — "*write the description from the artifact rather than from the intention*" — recommitted in the passage the fourth edition calls "this edition's centre," by generalizing **one** early artifact into the class's norm. An adopter builds the weaker form (a designed mutable line inside a frozen file — the §5.4 shape) believing it is the practice. Partial mitigation on the record: B.1's confirmation row for §3.3 is honestly marked "owed from both seats"; the two cold readers verified the passage's *ambiguity*, not its *accuracy*, and the doc says so. The claim as written is still false of 9 of 13 seals. *(My own finding.)*

### F4 — MINOR · C2.1/C2.3 — §1.6's "listed two leads as 'not yet activated'": the artifact lists three
**Doc:** §1.6 — "at the audit it still listed **two leads** as 'not yet activated' while one of them was eleven volumes deep."
**Ground truth:** `agents/journals/INDEX.md` lists **three** leads Not yet activated — `architect_docs_lead` (first spawn: M1), `rtl_lead` (M2), `dv_lead` (M2) — all three long activated at the audit (5, 3 and 11 volumes deep respectively). The posture list's C-32 evidence cell sampled only rtl_lead and dv_lead; PROCESS transcribed the cell instead of re-deriving from the file — the exact "quoted, not re-derived" habit §3.9 legislates against ("Import the rules; **re-derive the numbers**") and B.9 refusal 3 convicts elsewhere ("the shared-scope arithmetic quoted rather than re-derived in the passage that says *compute it*"). *(Sal C2.1 claim-sampling probe.)*

### F5 — MINOR · C2.1 / C9 (self-compliance) — Annex A.8's "and nowhere else" is false, known-false for two editions, and still in the running line unmarked
**Doc:** A.8 — tier assignment "is **declared per seat in the launcher definitions and nowhere else**."
**Ground truth:** `ORG_CHART.md`'s Roster table carries a **Model** column naming Opus 5 / Sonnet per seat, and the mermaid boxes repeat it ("ARCHITECT & DOCS LEAD — Opus 5", "rtl_module_dev — Sonnet ×N"); PROCESS §1.6's own roster facsimile — "written from this program's own two files" — shows the Tier column, contradicting A.8 within the same document.
**Aggravation:** The defect was filed by the round-3 record audit and is named in **both** B.9 refusal 3 and B.10 refusal 4 as unapplied — yet the running line stands unqualified, violating the document's own margin rule ("**the running line carries the current truth and the margin carries the history, never the reverse**") and its B.9-refusal-3 corollary ("a revision that leaves a filed defect unmarked beside its own new text has … hidden something" — A.8 is not one of the two it marked in place). The seam column cannot warn here: A.8 is `· 0`. *(Sal C9.1/C2.1; the two-editions-standing aggravation is my own reading.)*

### F6 — MINOR · C1.6 / C2.4 (feeds C5.3) — The kit's named "packet forms" original still teaches the folded ratio the document's own scoring law outlaws
**Doc:** §6.0 kit table — "**The packet forms** | The taxonomy of §3, as skeletons with their required sections | this repository, packet directory"; §3.8 — "a sign-off's reporting form is not allowed to force a fold"; §3.9 — "no ratio stands in for the dispositions."
**Ground truth:** `agents/handoffs/README.md:37` — the SO- skeleton demands "**Mutation kills**: <auditor-seeded mutations killed: **N/N**>" — the pre-ADR-0020 form that PROTOCOL §7(b.2) now forbids ("no ratio stands in for the dispositions … no `N/N` figure is read as coverage") and that the record's one sign-off deliberately refused to produce (§3.8's five-columns exhibit). The document exports the pointer to this original without flagging the drift; an adopter who pulls the named original inherits a form the constitution it sits beside has outlawed. This is §2.4's "a rule and its check that disagree" living inside the export unit's own kit row. *(My own finding.)*

### F7 — MINOR · C1.5 (depth floor) — The large-file gate's designed remedy is omitted everywhere
**Doc:** §2.6 R11 row and Annex A.7 state the refusal (1,000,000 bytes, journals carved out) and its anchor. **Ground truth:** both `agent_commit.sh` and `check_journals.sh` refusal messages name the sanctioned route — "large data ships as **fetch script + checksum manifest** — blob gate, ADR-0002" — and `grep 'fetch script\|checksum manifest' docs/PROCESS.md` returns nothing. ADR-0017 D1's journal carve-out reasoning ("the gate's remedy cannot apply to a file R2 requires in every commit") is half-carried (§2.6 gives the R2/R3 reason) but the remedy itself never appears, so an adopter hits the refusal with no stated lawful path for large data — a C1.3-style unhappy path whose resolution the record has and the document dropped. *(Sal C1.5 probe.)*

### F8 — NOTE · C7.1 — Micro-overclaim on [MC · C-38]: the literal empty marker is not machine-required
**Doc:** §2.1 — "the entry's file list must be **the empty marker** `[MC · C-38]`," and the facsimile's `- (none)` comment. **Ground truth:** `policy.sh`'s `extract_files_list` treats `- (none)` and an item-less `### Files-in-this-commit` section identically (both yield empty output); the machine checks set-equality-with-empty plus section presence — the literal marker is template convention. The only tier inflation I found anywhere, and it is a hair's width. *(Sal C7.1 matrix.)*

### F9 — NOTE · C7.1 — The worker row's "(its own return log only)" sits inside the machine-checked column
**Doc:** §2.3 facsimile, worker row: "its line's tree, narrowed further by its own packet; **packets/** (its own return log only)*" — inside a table introduced by "checked at commit time and re-checked in continuous integration `[MC · C-47]`." **Ground truth:** `policy.sh` grants every worker the whole of `agents/handoffs/*`; the own-return-log-only narrowing is packet/review-enforced. The adjacent residue paragraph names the line-tree narrowing residue and can be read to cover this, but the parenthetical rides in the MC column without its own tier — an inherited imprecision (PROTOCOL §6's table does the same). *(Sal C7.1.)*

### F10 — NOTE · C10 (disclosed staleness, confirmed real) — B.2 item 5's "Named, not performed" was performed mid-round at `22c60fb`
**Ground truth:** `22c60fb` (auditor, `J-auditor-0026`, one of the three declared sibling landings) appended "§7. APPENDED DATED NOTES" to the posture list — Notes 1 and 2 are exactly the two cures B.2 item 5 lists as "Named, not performed" (the C-126 12/4→15/8 cell and the C-117 anchor), performed in the idiom the doc prescribes (appended dated notes, no rewritten cells). The §7 notes state "They do not re-measure the list," so B.2 item 8's re-measurement debt remains genuinely open. The document's self-imposed blindness ("this seat did not read it, then or now … may be stale by one landing") was therefore accurate in both directions — item 5's cells are stale, item 8's are not. Recorded as verification that the disclosure mechanism worked, and that a sixth edition owes the item-5 row an update. *(My own check of the doc's declared blind spot.)*

### F11 — NOTE · C2.1 — Two small false-uniqueness/false-singularity claims
(a) §1.6: the roster is "the **only** artifact that enumerates the seats in one place" — `scripts/policy.sh`'s `KNOWN_AGENTS` line enumerates all ten seat identities (contingent seat included), machine-readably; same shape as the "calls itself unique" defect §1.4(c) corrects about read restrictions. (b) §2.1 facsimile: "may not be duplicated … **the commit gate refuses, and the pushed-history re-check refuses again**" — for *duplication* only CI refuses (`check_journals.sh` duplicate-key check); the commit gate prevents duplication by construction (it writes the trailers) and refuses protected keys only as `--extra-trailer`s. Both surfaces hold the property; the verb "refuses" is true of one. *(Sal C2.1.)*

---

## What I looked for and did not find
Per Sal's framework, run and returned empty: **no enforcement-tier upgrade on any sampled [MC] stamp** (C7.1 — the highest-value negative result); **no invented mechanism** — the two candidates (the packet-minting blockquote, §2.7's fourth requirement) are explicitly marked as proposal/own-rule with no posture row; **no stale gate semantics** — §3.9's fifteen scoring bullets track ADR-0020's Mutation record clause for clause, including the three grounds, the open-list/closed-duty rule, the equivalence and unreachable-set clauses, and the pre-exclusion floor; **no unresolvable cross-reference** in the sampled set; **no self-count that failed re-derivation** (sentinel, seam, pointers, headings, refs, seals, cases, rows-vs-numbers all reproduce). The document's own confessions — dormant compensating tier, never-instantiated escape ledger, never-run drill, unbuilt drift check, single-seat fifth edition — all verified true as stated, and in the one place its blindness mattered (the posture list moving at `22c60fb`) its staleness disclosure was exact.

## Bottom line
The document's central claim, as the fifth edition itself has re-scoped it — *the explaining half of an export unit, complete on mechanism, project-agnostic in §1–§6, replicable only as the unit* — **substantially survives contact with the record**, and its posture-stamp grammar is used honestly: I could not manufacture a single false [MC], and the [P1]/[PLANNED]/[UNANCHORED] stamps I traced all resolve as marked. What the audit did find is concentrated in one stratum the stamps structurally cannot reach: **claims about artifacts and their cross-document agreement** — the sponsor guide missing the fifth power its own kit row promises (F1), the unrouted constitutional falsehood (F2), the seal form generalized from a four-instance minority (F3), the kit's outlawed N/N relic (F6) — which is precisely where the document's §6.1 says its false claims cluster ("artifacts merely testify"), a prediction this audit confirms against the edition that made it. No finding is CRITICAL; F1–F3 are repairable without restructuring (one guide edit, one B.2 row, one paragraph rewritten from the later seals). Verdict in the program's own idiom: **FIT WITH REPAIRS** — and the repairs can be cut directly from the ledger above.
