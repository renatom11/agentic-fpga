THE CONTRARIAN — the fatal flaw is real, and it is not on any of the three fix lists.

1. THE EXPORT UNIT IS THE ONLY CONTROL IN THIS ORGANIZATION THAT HAS NEVER BOUNCED A LIVE TEST. The document's own standard is live fire: "An unverified protective setting is a belief" (§6.2 Step 3); branch protection had to bounce a real push before it counted. Apply that standard to the document itself: the claim "replication-grade" has zero replications behind it. Nobody — not the authors, not the auditor, not the confirmation round — has ever executed §6.2 in a clean repository. Bill's tabletop is the closest thing to a live fire this artifact has ever received, and it broke at Step 0 (shell unlocatable, his F1), then at Step 1/Step 2 (the adoption order contradicts §1.7 AND the program's own G0 record, his F2), then at the DoD (the acceptance arithmetic is uncomputable, his F12). Three consecutive failures at the front door are not three bugs; they are proof the adoption chapter was written from introspection, never rehearsed. The only correct condition the council can set is a test, not an edit: a party who did not write the document executes §6.2 against a named shell in a clean repo, and the breakage log becomes the next edition's input. Any "PASS WITH CONDITIONS" whose conditions are sentence-level repairs ratifies a bootstrap that has still never run.

2. THE STAMP APPARATUS MEASURED A DIFFERENT DOCUMENT, AND THE TEXT HIDES THIS. My own finding, from the posture list's header: the 128-row measurement that "is the source of this edition" (Read this first, line 26) was taken over PROCESS.md at 6c02f5b — 1,445 lines. The document before the council is 2,973 lines. More than half the text postdates its own evidentiary spine. The preamble manages this exposure with a claim that is demonstrably false: "Failure-class prose is not stamped... This is deliberate and is THE ONE PLACE a reader will see unstamped normative-sounding text" (lines 87–90), and "Exactly TWO rules are added that way" without rows (line 96). Bob's F1 refutes both boundaries: §2.7's "its owed test cases still unapplied" is unstamped, normative-sounding, false — and it sits in the same paragraph as the true stamped limb [RE · C-69], borrowing the stamp's authority. That is the mechanism of the residual dishonesty: the stamps are individually honest (Bob's 25-claim sample held), but the two-tier text they create lets unmeasured prose ride adjacent to measured prose with no visible seam, and the preamble's coverage claims ("the one place", "exactly two") assert a seam that does not exist. Sal's C7 warns about controls presented as mechanical; this is the same failure one level up — testimony presented as measurement by proximity.

3. THE EDITION RE-COMMITS THE EXACT DEFECT CLASS IT EXISTS TO PURGE, THREE TIMES, WHICH IS A PROCESS VERDICT, NOT AN ERRATA LIST. The second edition's founding conviction was the first edition's unqualified quantifiers (§2.5's own narrative). Now count: §4.7 "deliberately small and COMPLETELY ENUMERATED" over a sponsor surface missing the harvest veto (Bob F3, verified against PROTOCOL §7); §3 "The taxonomy is CLOSED, and this is it" against a record containing HT- transits, campaign packets, adjudication addenda (Bob F5, Charlie F5); "the one place... unstamped" against §2.7's false limb (point 2 above). One recurrence is an error. Three recurrences of the promised-purged class, in an edition produced specifically to purge it, means the production process cannot detect this class — and the council should say so rather than file three findings.

4. WHO GRADED THIS HOMEWORK? THE DOCUMENT FAILS ITS OWN ROOT CLASS. §5.7 names the root failure: the author grading its own homework. §1.4(e) — added as this round's ship-blocker — requires any model that grades to be externally anchored. Now audit the document's own provenance chain: written by the specification lead, stamped by the program's own auditor (J-auditor-0023), confirmed in B.1 by a procedure in which "each seat confirms the description of ITS OWN discipline" — literally each author signing off on the account of its own homework — and, per B.6's own confession, "It has never been through the packet lifecycle it describes": no work order, no countersignature, dispatch-only, no external anchor anywhere. The export flagship was produced outside the process it exports, and the falsehoods the council's reviewers found (F1, F3, F5, the DV-escape ledger hole, model tiers, federation) are precisely what that in-house confirmation round missed. This council is the document's first external anchor, and the anchor moved on first contact. The document's strongest section, per Charlie, is its rationale prose (C8) — the one layer that is unfalsifiable. It is strongest exactly where it cannot be checked. That pattern — accurate where scripts refuse, false where artifacts testify — is the document's OWN stated law (§6.1), and PROCESS.md is an artifact.

5. BILL'S "WITH THE SHELL IN HAND, F1/F6 DISSOLVE" IS WRONG, AND THE COUNCIL MUST NOT INHERIT IT. Naming the shell converts an absence into a silent contradiction, which §2.4 itself rates worse. The record shows the shell is ALREADY thirty-one commits ahead, governed by a federation law (outbox packets, inbox PRs, "no privileged lane") that PROCESS.md never mentions, while §6.0 asserts the shell is "kept deliberately frozen except for one commit per lessons harvest" — false against commit 7fb2c99 (Bob F4). And §6.0 concedes the doc-shell drift check "does not exist yet." So the export unit's two halves demonstrably disagree TODAY, with no instrument to detect it. An adopter handed both halves gets a document describing machinery the shell no longer is. The shell must be named AND the drift check must exist AND the federation transit must be documented before C5 can score above 2 — these are conjunctive, not alternative, repairs.

6. THE DOCUMENT HAS NO SURVIVAL MECHANISM, AND ITS ACCURACY HALF-LIFE IS MEASURED IN HOURS. Bob F12: Annex B.1's "owed" cells were stale within hours of landing (J-orchestrator-0266 closed them the same day). No rule binds PROCESS.md into the §2.7 amendment procedure — an ADR that changes enforcement semantics obliges a self-test case but not a PROCESS re-edition (Bob F12, Charlie Finding 2, independently). The organization amends constantly (21 ADRs, one PROPOSED mid-flight that will flip five posture rows the moment it lands, per B.3). §5.1 is the document's own theorem: "remedies decay without mechanical checks." PROCESS.md is a remedy with no mechanical check. Under C10 this is not housekeeping: for a document whose entire authority model is "a stamp is a measurement at one commit," being unanchored to any commit (no edition line, no date, no hash — both cold readers confirmed) and unbound to any re-edition trigger means it is DESIGNED to become false silently. It was already false at §2.7 on the day it shipped.

7. VERDICT AND THE ONE QUESTION THE COUNCIL IS AVOIDING. Under Sal's own mechanics: C5 as it stands is a 1–2 (Step 0 unexecutable, genesis contradictory, acceptance test uncomputable — the mission criterion fails at its first, middle, and last step), and C2 sits at the veto boundary (an execution-verified false record-claim in §2.7, two false quantifier claims, a false frozen-shell claim). Sal's rule: FAIL if C5 below 2 or any veto-class below 3. The honest verdict is FAIL — or, if the council prefers PASS WITH CONDITIONS, the conditions must be structural and testable, ranked: (1) name the shell and document the federation transit that actually moves the export; (2) an independent party executes §6.2 end-to-end in a clean repo and the log is folded in — reconciling §6.2 with §1.7 and G0 in the process; (3) correct §2.7's false limb and the two false quantifiers, and delete or prove the preamble's "one place"/"exactly two" coverage claims; (4) export the charter MECHANISM layer (DV-escape ledger, relay-sampling cadence, canary interface — Bob F2, Bill F3) and the model-tier invariant; (5) an edition anchor line plus a standing rule binding PROCESS re-editions into §2.7's amendment procedure; (6) the doc-shell drift check, which §6.0 itself nominates as "the most useful thing an adopter could build first" — it is more useful still for the exporter. The question the council is avoiding: this document's first line commands the reader, "Do not import any statement in this document that a control is mechanically enforced. Check it in your own machinery before you repeat it." The document's claim about ITSELF — that it is replication-grade — is exactly such a statement, and nobody, including its authors, has checked it in any machinery. Until someone has, approving this document means importing an unverified guarantee — the failure of §2.5's declared external dependency, one level up, in the document's own words, about the document itself.
